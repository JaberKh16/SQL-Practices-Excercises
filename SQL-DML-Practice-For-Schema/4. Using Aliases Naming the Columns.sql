select emp_no EMPLOYEE_ID
from employees;

select emp_no , (first_name)  'First_Name'
from employees;

select emp_no ,dept_no 'DEPARTMENT ID'
from dept_emp;

select dept_name as DEPARTMENT_NAME
from departments;

select title as 'TITLE_DESCRIPTION'
from titles;
